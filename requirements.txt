# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# Data processing
numpy==2.4.6

# ML/Embeddings — pinned to the Linux-proven set (see requirements_web.txt).
# An unpinned torch pulls the newest release and breaks fresh installs.
torch==2.13.0
sentence-transformers==5.6.1
transformers==5.14.1
tokenizers==0.22.2
huggingface_hub==1.25.1
scikit-learn==1.9.0
scipy==1.17.1
faiss-cpu==1.14.3

# PDF processing
PyMuPDF>=1.23.0

# ArXiv API
arxiv>=2.0.0

# LLM APIs
anthropic>=0.18.0

# Database
# (Using SQLite - included in Python standard library)

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Web demo (Días 10-12 del sprint)
# gradio>=4.0.0

# LeanDojo (D3 — extracción de premisas)
# Se instala en WSL2 (Linux), no en Windows nativo.
# Ver src/novelty_v2/dimensions/d3_premises.py y paper/decisions.md.
# leandojo>=1.8.0  ← descomentar solo dentro del entorno WSL2

# Development
black>=23.0.0
flake8>=6.1.0
mypy>=1.6.0
