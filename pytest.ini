[pytest]
markers =
    live: tests that hit live external APIs (Leandex, Semantic Scholar, ArXiv, Anthropic). Exclude with `pytest -m "not live"`.
    slow: tests that run ExtractData or other slow operations (D3 integration). Exclude with `pytest -m "not slow"`.
