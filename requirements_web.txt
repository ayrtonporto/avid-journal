# AViD Journal — Web/demo runtime dependencies.
# Used by deploy/Dockerfile (pip install -r requirements_web.txt).
# Runtime-only subset of requirements.txt (no black/flake8/mypy/pytest).

# ── Core ────────────────────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
numpy==2.4.6

# ── ML / embeddings (D1 similarity, MiniLM) ─────────────────────────────
# Pinned to the exact set proven working in the Linux Docker image. These float
# fast and break on fresh installs: an unpinned `torch` pulls the newest release
# (which may drop CPU wheels or need a newer toolchain) and numpy 2.x churns ABI.
# If you must bump, change all of these together and rebuild from a clean image.
torch==2.13.0
sentence-transformers==5.6.1
transformers==5.14.1
tokenizers==0.22.2
huggingface_hub==1.25.1
scikit-learn==1.9.0
scipy==1.17.1
faiss-cpu==1.14.3

# ── PDF + arXiv (D1 existence) ──────────────────────────────────────────
PyMuPDF>=1.23.0
arxiv>=2.0.0

# ── LLM API (Anthropic provider) ────────────────────────────────────────
anthropic>=0.18.0

# ── Web server + demo UI ────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gradio>=4.0.0
python-multipart>=0.0.9
