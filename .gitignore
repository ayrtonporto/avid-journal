# Python
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.eggs/
dist/
build/

# Entorno / secretos
.env
.env.*
!.env.example

# Herramientas / IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Tests / cobertura
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Logs locales de ejecución
*.log

# Stray results / temp files en raíz
arxiv_verification_*.json

# Project briefing (internal doc)
IDEA.md

# Cache local y auditorías de desarrollo
cache/
audit/

# Claude Code: worktrees y configuración local (pero NO los docs)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Carpeta huérfana de Claude Code (typo histórico)
.claire/

# Proyectos Lean standalone generados por el orquestador (--standalone / tests)
lean_papers/

# Copia local de Numina (el oficial va como submodulo en vendor/numina-lean-agent)
Numina Lean Agent/

# Lean (por si .gitignore del subproyecto no aplica en algún caso)
.lake/

# LeanDojo — caché de trazas de mathlib (puede pesar 10-20 GB)
# En Windows: no aplica (LeanDojo corre en WSL2)
# En WSL2: ~/.cache/leandojo/ y ~/.cache/huggingface/ viven dentro del
# filesystem de WSL, no en el repo, así que no son problema acá.
# Si alguna vez se guarda una traza local dentro del repo, excluirla acá:
lean_dojo_cache/
*.olean
# (los .olean del lean_project/ están cubiertos por lean_project/.gitignore)
TestLemmas.lean
