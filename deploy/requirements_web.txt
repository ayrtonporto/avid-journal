# AViD Journal — Web demo dependencies (full pipeline: formalization + D2 + D1)
# Used by Docker deployment. Includes Gradio for UI.

gradio>=4.0.0,<5.0.0
requests>=2.31.0
python-dotenv>=1.0.0
numpy>=1.24.0
sentence-transformers>=2.2.0
arxiv>=2.0.0
