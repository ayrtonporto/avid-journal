# AViD Journal — Web demo dependencies (full pipeline: formalization + D2 + D1)
# Used by Docker deployment. Includes Gradio for UI + FastAPI for auth.

gradio>=4.0.0,<5.0.0
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6
requests>=2.31.0
python-dotenv>=1.0.0
numpy>=1.24.0
sentence-transformers>=2.2.0
arxiv>=2.0.0
